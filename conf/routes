# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                    controllers.Application.index

POST        /users/login         controllers.UserCtrl.login

POST        /users/logout        controllers.UserCtrl.logout

GET         /test               controllers.Application.test
#
#POST        /users/login        controllers.UserCtrl.login()
#
#POST        /chats              controllers.ChatCtrl.sendMessageOld()
#
#GET         /chats              controllers.ChatCtrl.fetchMessages(userA: Long, userB: Long, since: Long?=0)
